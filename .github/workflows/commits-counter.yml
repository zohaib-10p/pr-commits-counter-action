name: Count Commits

on: push

jobs:

  count-commits:
    runs-on: ubuntu-latest
    name: Count the commits
    steps:
      - name: Count Commits
        uses: zohaib-10p/testing-github-action@main
        with:
          owner: ${{ github.repository_owner }}
